'use client'; import {useState} from 'react';
export default function Join(){const [pin,setPin]=useState('');const [nickname,setNickname]=useState('');const [joined,setJoined]=useState(false);const [error,setError]=useState('');async function join(){setError('');const response=await fetch('/api/sessions/join',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({pin,nickname})});const data=await response.json();if(!response.ok){setError(data.error==='INVALID_PIN'?'That code is not active. Try again.':data.error);return}sessionStorage.setItem('summit-player',JSON.stringify(data));setJoined(true)} return <main className="join"><div className="join-card"><p className="eyebrow">READY TO CLIMB?</p><h1>{joined?'You’re in.':'Enter your'}<br/><em>{joined?'Find your route.':'game code.'}</em></h1>{!joined&&<><input autoFocus inputMode="numeric" maxLength={6} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,''))} placeholder="000 000" aria-label="Game PIN"/><input maxLength={24} value={nickname} onChange={e=>setNickname(e.target.value)} placeholder="Your nickname" aria-label="Nickname"/><button className="primary" disabled={pin.length!==6||!nickname.trim()} onClick={join}>Join climb <span>↗</span></button></>}{joined&&<p className="muted">You’re waiting for the host to start the expedition.</p>}{error&&<p className="error">{error}</p>} {!joined&&<p className="muted">Ask your host for the six-digit PIN or scan the room QR code.</p>}</div></main>}
