'use client';import {useEffect,useState} from 'react';import type {Locale} from '@/lib/i18n';
export default function LanguageSwitcher(){const [locale,setLocale]=useState<Locale>('en');useEffect(()=>{const saved=localStorage.getItem('summit-locale') as Locale|null;if(saved)setLocale(saved)},[]);function change(value:Locale){setLocale(value);localStorage.setItem('summit-locale',value);window.dispatchEvent(new CustomEvent('summit-language',{detail:value}))}return <select aria-label="Language" className="language-select" value={locale} onChange={e=>change(e.target.value as Locale)}><option value="en">EN</option><option value="ru">RU</option><option value="et">ET</option></select>}
