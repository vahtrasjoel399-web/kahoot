'use client';import {useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Login(){const [email,setEmail]=useState('');const [sent,setSent]=useState(false);const [error,setError]=useState('');async function submit(e:React.FormEvent){e.preventDefault();try{const {error}=await createClient().auth.signInWithOtp({email});if(error)throw error;setSent(true)}catch(e){setError(e instanceof Error?e.message:'Unable to send login link')}}return <main className="join"><form className="join-card" onSubmit={submit}><p className="eyebrow">HOST ACCESS</p><h1>Lead the<br/><em>expedition.</em></h1>{sent?<p className="muted">Check your email for the secure sign-in link.</p>:<><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="teacher@email.com" aria-label="Email"/><button className="primary">Send login link <span>↗</span></button></>}{error&&<p className="error">{error}</p>}</form></main>}
